# 🧮 Матричный калькулятор на Go

Консольное приложение для выполнения базовых операций с матрицами. Идеально подходит для студентов, изучающих линейную алгебру и программирование на Go.

## 📊 Возможности

✅ **Сложение матриц** - одинакового размера  
✅ **Умножение матрицы на число** (скалярное умножение)  
✅ **Умножение матриц** - с проверкой совместимости размеров  
✅ **Интерактивный ввод** - с проверкой корректности данных  
✅ **Поддержка размеров** - 2x2 и 3x3 матрицы  
✅ **Наглядный вывод** - форматированный вывод результатов  

## 🚀 Быстрый старт

### 1. Установка и запуск
```bash
# Сохраните код в файл matrix_calc.go
# Убедитесь, что установлен Go

# Запустите программу:
go run matrix_calc.go

# Или скомпилируйте:
go build matrix_calc.go
./matrix_calc
```

### 2. Главное меню
При запуске появится меню:
```
=== КАЛЬКУЛЯТОР МАТРИЦ ===
1. Сложение матриц
2. Умножение матрицы на число
3. Умножение матриц
4. Выход
Выберите операцию:
```

## 📝 Использование

### Выбор размера матрицы
Для любой операции сначала выбирается размер:
```
Выберите размер матрицы (1 - 2x2, 2 - 3x3):
```

### Ввод матрицы
Вводите числа через пробел. Пример для матрицы 2x2:
```
Ввод матрицы A (2x2):
Строка 1 (введите 2 чисел через пробел): 1.5 2.3
Строка 2 (введите 2 чисел через пробел): 3.7 4.1
```

## 🧮 Примеры операций

### 1. Сложение матриц (A + B)
```
Матрица A:
    1.00    2.00
    3.00    4.00

Матрица B:
    5.00    6.00
    7.00    8.00

Результат сложения (A + B):
    6.00    8.00
   10.00   12.00
```

### 2. Умножение матрицы на число (A × k)
```
Матрица A:
    1.00    2.00
    3.00    4.00

Число: 2.5

Результат умножения (A × 2.50):
    2.50    5.00
    7.50   10.00
```

### 3. Умножение матриц (A × B)
```
Матрица A (2x3):
    1.00    2.00    3.00
    4.00    5.00    6.00

Матрица B (3x2):
    7.00    8.00
    9.00   10.00
   11.00   12.00

Результат умножения (A × B):
   58.00   64.00
  139.00  154.00
```

## ⚙️ Технические детали

### Структура Matrix
```go
type Matrix struct {
    Rows int          // Количество строк
    Cols int          // Количество столбцов
    Data [][]float64  // Двумерный массив с данными
}
```

### Основные функции
- `NewMatrix(rows, cols int)` - создает новую матрицу
- `InputMatrix(rows, cols int, name string)` - читает матрицу с клавиатуры
- `Add(a, b *Matrix)` - сложение двух матриц
- `MultiplyByScalar(a *Matrix, scalar float64)` - умножение на скаляр
- `Multiply(a, b *Matrix)` - умножение матриц
- `Print()` - форматированный вывод матрицы

## 🔍 Проверки и валидация

Программа проверяет:
- Правильность ввода чисел (целые и дробные через точку)
- Совпадение размеров матриц для сложения
- Совместимость размеров для умножения матриц
- Корректность выбора в меню


6. **Форматированный вывод** - printf с указанием точности

### Алгоритмы:
- **Сложение матриц**: O(n²) - двойной цикл по элементам
- **Умножение на скаляр**: O(n²) - двойной цикл
- **Умножение матриц**: O(n³) - тройной вложенный цикл

## ⚠️ Ограничения

- Поддерживаются только матрицы 2×2 и 3×3
- Нет поддержки отрицательных индексов
- Нет сохранения результатов в файл
- Нет операций транспонирования, определителя и обратной матрицы



### Продвинутые улучшения:
1. **Оптимизировать умножение матриц** (алгоритм Штрассена)
2. **Добавить вычисление обратной матрицы**
3. **Реализовать решение систем линейных уравнений**
4. **Добавить графический интерфейс**

## 💡 Советы по использованию

### Для проверки работы:
```bash
# Тест 1: Сложение
A = [[1, 2], [3, 4]]
B = [[5, 6], [7, 8]]
A + B = [[6, 8], [10, 12]]

# Тест 2: Умножение на скаляр
A = [[2, 3], [4, 5]]
k = 3
A × k = [[6, 9], [12, 15]]

# Тест 3: Умножение матриц
A = [[1, 2], [3, 4]]
B = [[2, 0], [1, 2]]
A × B = [[4, 4], [10, 8]]
```

### Формат ввода чисел:
- Целые числа: `1`, `-5`, `42`
- Дробные числа: `3.14`, `-2.5`, `0.75`
- Несколько чисел в строке: `1 2.5 -3 4.2`

## 🐛 Отладка

### Распространенные ошибки:
```
1. "Ошибка: нужно ввести ровно X чисел" 
   → Введите правильное количество чисел через пробел

2. "Ошибка: 'abc' не является числом"
   → Используйте только цифры, точку и минус

3. "матрицы должны быть одного размера"
   → Для сложения матрицы должны быть одинаковыми

4. "число столбцов первой матрицы должно равняться числу строк второй"
   → Проверьте размеры матриц для умножения
```

```

**Умножение на скаляр:**
```
C[i][j] = k × A[i][j]
```

**Умножение матриц:**
```
C[i][j] = Σ(A[i][k] × B[k][j])
где k = 1..n
```


---

**💡 Совет:** Сохраняйте результаты вычислений в тетради для сверки с теоретическими расчетами на занятиях по линейной алгебре!
